import PlaceholderPage from "@/components/PlaceholderPage"

export default function Triggers() {
  return (
    <PlaceholderPage title="Triggers" description="Events that kick off workflows and AI actions." />
  )
}
